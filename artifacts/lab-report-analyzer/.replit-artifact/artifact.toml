kind = "web"
previewPath = "/"
title = "Lab Report Analyzer"
version = "1.0.0"
id = "artifacts/lab-report-analyzer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25862

[services.development]
run = "pnpm --filter @workspace/lab-report-analyzer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lab-report-analyzer", "run", "build" ]
publicDir = "artifacts/lab-report-analyzer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25862"
BASE_PATH = "/"
